import { sum } from "./utils/math";
import { time, timer } from './utils/format'

console.log(1)
console.log(time, timer)

const message: string = "Hello TypeScript";
console.log(sum(20, 30));
console.log(message);
