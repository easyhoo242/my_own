// 命名空间
// 在ecma拥有自己的导出方式之前 使用的模块化方法

export namespace time {
  export const format = (name: string) => {
    return name
  }

  export const name = 'dc'
}

export namespace timer {
  export const format = (name: string) => {
    return name
  }

  export const name = 'dc'
}